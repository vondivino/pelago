
import 'package:flutter/material.dart';

class MatcherScreen extends StatelessWidget {
  const MatcherScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Matcher screen'),
    );
  }
}